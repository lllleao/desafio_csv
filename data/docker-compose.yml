services:
  database:
    container_name: 'mysql-db'
    image: mysql:9.5.0
    restart: always
    env_file:
      - ../.env
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
volumes:
  mysql_data:
